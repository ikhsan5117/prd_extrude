@model VelastoProductionSystem.Models.ElwpMachine
@{
    ViewData["Title"] = "Tambah Mesin Extrude";
}

<style>
    .form-glass-card {
        background: rgba(30, 34, 41, 0.7);
        backdrop-filter: blur(15px);
        border-radius: 20px;
        border: 1px solid rgba(255, 255, 255, 0.08);
        padding: 32px;
        max-width: 700px;
        margin: 0 auto;
    }
    .form-label { color: #94a3b8; font-size: 0.85rem; font-weight: 600; text-transform: uppercase; margin-bottom: 8px; }
    .form-control, .form-select {
        background: rgba(0, 0, 0, 0.2) !important;
        border: 1px solid rgba(255, 255, 255, 0.1);
        color: #fff !important;
        border-radius: 12px;
        padding: 12px;
    }
    .btn-submit {
        background: #fbbf24;
        border: none;
        color: #000;
        font-weight: 700;
        padding: 12px 24px;
        border-radius: 12px;
    }
</style>

<div class="container py-5">
    <div class="form-glass-card">
        <h3 class="text-white fw-bold mb-4"><i class="bi bi-plus-circle text-warning me-2"></i> Tambah Mesin Baru</h3>
        
        <form asp-action="Create" method="post">
            <input type="hidden" asp-for="PlantId" />
            
            <div class="row g-4">
                <div class="col-md-6">
                    <label class="form-label">Kode Mesin</label>
                    <input asp-for="KodeMesin" class="form-control" placeholder="Contoh: EXT-DL4" required />
                </div>
                <div class="col-md-6">
                    <label class="form-label">Area Produksi</label>
                    <select asp-for="AreaId" class="form-select">
                        <option value="1">Extrude</option>
                        <option value="10">Extrude TPE</option>
                    </select>
                </div>
                <div class="col-md-12">
                    <label class="form-label">Nama Mesin</label>
                    <input asp-for="NamaMesin" class="form-control" placeholder="Nama Lengkap Mesin" required />
                </div>
                <div class="col-md-6">
                    <label class="form-label">Kapasitas (pcs/jam)</label>
                    <input asp-for="Kapasitas" type="number" class="form-control" />
                </div>
                <div class="col-md-6">
                    <label class="form-label">Keterangan</label>
                    <input asp-for="Keterangan" class="form-control" />
                </div>
                
                <div class="col-12 mt-4 pt-2">
                    <div class="d-flex gap-3 justify-content-end">
                        <a asp-action="Index" class="btn btn-outline-light px-4">Batal</a>
                        <button type="submit" class="btn btn-submit px-5">Simpan Mesin</button>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>
